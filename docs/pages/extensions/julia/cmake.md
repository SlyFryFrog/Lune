# CMake

## Flags and Options

- `"BUILD_JULIA_SCRIPTING_SUPPORT"`: Enables Julia scripting support when to `ON`, defaults to `OFF`.
    - *Note:* You have to link to the shared library in your project's `CMakeLists.txt`
